# marketplace-whatsapp-hub

Deploy repo for the **whatsapp-hub** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs whatsapp-hub --push`.

Coolify: base_directory `/`, dockerfile `/apps/whatsapp-hub/Dockerfile`.
